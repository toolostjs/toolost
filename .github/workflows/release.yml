name: Release Publish

on:
  release:
    types:
      - published
  workflow_dispatch:
    inputs:
      release_tag:
        description: Release tag to publish (e.g. v1.2.3)
        required: true
        type: string
      prerelease:
        description: Publish with the dev npm dist-tag
        required: false
        default: false
        type: boolean

permissions:
  contents: read
  id-token: write

concurrency:
  group: release-${{ github.event.release.tag_name || github.event.inputs.release_tag || github.run_id }}
  cancel-in-progress: false

jobs:
  publish:
    name: Publish to npm
    runs-on: ubuntu-latest
    environment: release

    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Setup pnpm
        uses: pnpm/action-setup@v6
        with:
          version: 10

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: 22.x
          cache: pnpm
          registry-url: https://registry.npmjs.org

      - name: Update npm to latest
        run: npm install -g npm@11.6.0

      - name: Install dependencies
        run: pnpm install --no-frozen-lockfile

      - name: Verify project quality
        run: |
          pnpm run typecheck
          pnpm run test
          pnpm run build

      - name: Validate release tag matches package version
        env:
          RELEASE_TAG: ${{ github.event.release.tag_name || github.event.inputs.release_tag }}
        run: |
          PACKAGE_VERSION="$(node -p "require('./package.json').version")"
          NORMALIZED_TAG="${RELEASE_TAG#v}"

          if [ "$NORMALIZED_TAG" != "$PACKAGE_VERSION" ]; then
            echo "Release tag ($RELEASE_TAG) must match package.json version ($PACKAGE_VERSION)." >&2
            exit 1
          fi

      - name: Determine npm dist-tag
        id: dist_tag
        env:
          IS_PRERELEASE: ${{ github.event.release.prerelease || github.event.inputs.prerelease }}
        run: |
          if [ "$IS_PRERELEASE" = "true" ]; then
            echo "tag=dev" >> "$GITHUB_OUTPUT"
          else
            echo "tag=latest" >> "$GITHUB_OUTPUT"
          fi

      - name: Publish package
        env:
          DIST_TAG: ${{ steps.dist_tag.outputs.tag }}
        run: npm publish --access public --provenance --tag "$DIST_TAG"
